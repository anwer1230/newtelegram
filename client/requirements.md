## Packages
framer-motion | Smooth animations and page transitions
lucide-react | Beautiful icons for the UI

## Notes
The application is entirely in Arabic (Right-to-Left).
I will programmatically set `document.documentElement.dir = 'rtl'` and `lang="ar"` in App.tsx.
